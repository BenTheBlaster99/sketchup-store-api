# No RewriteRule: Wasmer PHPIx returns "Htaccess evaluation failed" for them.
# Use PATH_INFO URLs on Wasmer, e.g. /index.php/admin and /index.php/api/...
# Local Apache can still use a custom vhost rewrite if needed.
